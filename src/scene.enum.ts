/**
 * A SceneEnum holds an enum of our scenes.
 *
 * It is intended to be used when instantiating / calling various Phaser scenes
 * in order to aid rapid game development and eventual refactoring.
 */
export enum SceneEnum {
    Game = 'GameScene'
}
